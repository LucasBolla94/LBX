// src/app/airdrop/page.tsx

'use client';

import NoPromo from "@/components/promo/NoPromo";

export default function AirdropPage() {
  return (
    <main className="flex items-center justify-center w-full min-h-screen bg-[var(--background)] p-6">
      <NoPromo />
    </main>
  );
}
